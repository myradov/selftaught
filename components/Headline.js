import React from 'react'
import styles from '../styles/Home.module.css'

const Headline = () => {
  return (
    <div className={styles.headline}>
        <h2>I kept always two books in my pocket, one to read, one to write in</h2>
    </div>
  )
}

export default Headline